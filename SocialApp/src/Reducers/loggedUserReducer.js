let initialstate={
        userid:1,
        username:'tst'
}

let loggedUserReducer=(state=initialstate, action)=>{
    switch(action.type){
        
        default:
            return state  
    }
}

export default loggedUserReducer;